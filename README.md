# 🔐 Password Generator (Python)

A simple and secure **Random Password Generator** built using Python.  
This program generates strong passwords containing uppercase letters, lowercase letters, numbers, and special characters.  
You can also customize the password length.

## 🚀 Features

✔ Generates strong, unpredictable passwords  
✔ Ensures at least one uppercase, lowercase, digit, and special character  
✔ Fully customizable password length  
✔ Uses Python’s built-in `string` module and `random` module  
✔ Beginner-friendly and easy to modify  
✔ Clean, readable code  
